# 🏛️ 가상 국가 관리 시스템 시스템 명세서 (SYSTEM_SPEC.md)

이 문서는 본 가상 국가 관리 시스템의 모든 기능, 데이터 스키마 및 기술적 사양을 정의합니다. 다른 AI나 개발자가 이 명세서를 바탕으로 시스템을 동일하게 재구현할 수 있도록 상세히 작성되었습니다.

---

## 1. 개요
- **플랫폼:** Python Flask (Full-stack)
- **데이터베이스:** 사용 금지 (100% CSV 파일 기반 저장소)
- **실시간 기능:** Flask-SocketIO
- **자동화:** APScheduler
- **디자인:** SF/현대적인 블루 테마, 모바일 친화적 레이아웃, 하단 네비게이션

## 2. 핵심 기술 아키텍처

### 2.1 데이터 지속성 (CSV Persistence)
- 모든 데이터는 `data/` 디렉토리에 UTF-8-sig 인코딩의 CSV 파일로 저장됩니다.
- **동시성 제어:** `threading.Lock`을 사용하여 파일 쓰기 시 데이터 유실 및 충돌을 방지합니다.
- **실시간 반영:** 런타임에 CSV를 매번 읽어 들여 파일 시스템 수준의 직접 수정을 실시간으로 반영합니다.

### 2.2 플레이스홀더 시스템
- 법률 텍스트 및 등급 이름에 `{국가이름}`, `{화폐단위}` 플레이스홀더를 사용하여 `config.csv` 설정에 따라 동적으로 렌더링합니다.

---

## 3. 모듈별 기능 명세

### 3.1 회원 및 신원 관리
- **회원가입:** 이름, ID, PW, 생년월일, 학년/반 입력. 초기 등급은 '준회원'.
- **주민등록번호 발급:** `[설정프리픽스]-[학년/반코드]-[이름해시]` 조합의 고유 번호 생성.
- **주민등록증 생성:** Pillow 라이브러리를 사용하여 QR 코드가 포함된 카드 이미지(`.png`) 자동 생성.
- **학년/반 갱신:** 매년 3월 1일 이후 첫 로그인 시 정보 업데이트 강제 및 신분증 재발급.

### 3.2 경제 시스템
- **주급 및 세금:** 매주 자동으로 등급별 주급 지급 및 세율(tax_config.csv)에 따른 세금 징수.
- **국고(Treasury):** 모든 세금, 벌금은 국고로 귀속되며 예산 집행 시 차감.
- **은행:**
    - **계좌이체:** 이름과 **전화번호**를 모두 확인하여 동명이인 방지 및 보안 강화.
    - **대출:** 신용 점수 기반 한도 내 대출 및 이자 상환 시스템.

### 3.3 정치 및 입법
- **대통령 선거:** 3주 주기 자동 타임라인(후보등록 -> 확정 -> 투표 -> 개표).
- **정당:** 창설 신청, 승인, 가입/탈퇴 및 의석수 집계.
- **입법 프로세스:** 법안 발의 -> 입법부 투표 -> 대통령 최종 승인 -> `data/laws/` 내 `.txt` 파일 자동 생성.
- **예산안:** 예산안 발의 및 행정부/대통령 승인 시 국고 자금 즉시 집행.

### 3.4 사법 시스템
- **3심제 재판:**
    1. 고소/고발 접수.
    2. 1심(사법 1등급 판결).
    3. 2심(AI 판결 - 관리자 대행).
    4. 3심(최고 회의 최종 판결).
- **벌점 및 전과:** `penalties.csv`에 기록. 200점 도달 시 자동 정지 및 관리자 검토 큐 등록.

### 3.5 실시간 통신 및 소셜
- **채팅 (카카오톡 스타일):**
    - 1:1 DM 및 단체 대화방 지원.
    - 전화번호 기반 연락처 연동.
    - 욕설 필터링 및 도배 방지 로직.
- **SNS (인스타그램 스타일):**
    - 등급 높은 순서대로 게시물 상단 노출.
    - 좋아요, 댓글 및 사진 첨부 기능.

### 3.6 게임 및 엔터테인먼트
- **가위바위보/하이로우/룰렛:** 컴퓨터와의 확률 기반 베팅 게임.
- **슬롯머신:** 심볼 조합별 배율 보상.
- **국가 복권:** 매주 자동 추첨 및 국고 연동 당첨금 지급.
- **주사위 배틀:** 유저 간 실시간 SocketIO 대결.

---

## 4. 데이터 스키마 (주요 CSV)

| 파일명 | 컬럼 구성 |
| :--- | :--- |
| `users.csv` | name, login_id, password, birth, grade, phone, resident_id, school_info, assets, status, credit_score, last_update_year |
| `config.csv` | key, value |
| `treasury.csv` | timestamp, type, amount, balance, reason |
| `sns_posts.csv` | id, user, grade, content, image, timestamp, likes |
| `chat_rooms.csv` | room_id, type, name, members, last_message, timestamp |
| `emergency_records.csv` | type, starter, timestamp, status, end_timestamp |

---

## 5. 관리자 제어 권한
- **데이터 직접 수정:** CSV 데이터 에디터 및 성문법 에디터 제공.
- **계정 관리:** 승인 대기자 승인, 정지 계정 복구/삭제.
- **국가 비상사태:** 비상사태/계엄령 선포 및 해제 (정치/경제 활동 제한).
- **IP 차단:** 특정 IP의 접속을 `before_request` 수준에서 차단.
